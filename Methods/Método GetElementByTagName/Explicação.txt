O retorno do Get by tag name será focado em uma coleção de elementos HTML, diferente do ById q retornava apenas um elemento por vez. A diferença entre usar um Array normal e um HTMLCollection, será na parte de opções de uso para métodos, já q Array terá muito mais opções que o HTMLCollection usando o método GetElementByTagName. É possível converter o HTMLCollection usando o operador ...spread